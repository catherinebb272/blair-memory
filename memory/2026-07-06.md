# 2026-07-06

## Saltwash — beach-read print complete

Catherine uploaded the cleaned 18×24 master (`Beach-Read-18x24.jpg`, 5400×7232) post-Photoshop. She asked for crops from the right and bottom only (composition bias) and Lanczos downscale to the 4 standard derived sizes.

### What I built

- `saltwash/beach-read/` working dir + `print-sizes/` delivery batch
- Master: `saltwash-beach-read_18x24_300dpi.jpg` (5400×7200, 7.1 MB) — 32 px trimmed from the bottom of the source to land at clean 18×24
- Derived sizes, all under Etsy 20MB:
  - 16×24 (4800×7200, 6.4 MB) — 600 px trim, all from right
  - 11×14 (3300×4200, 3.5 MB) — 327 px trim, all from bottom
  - 8×10 (2400×3000, 2.1 MB) — 450 px trim, all from bottom
  - 5×7 (1500×2100, 1.1 MB) — 257 px trim, all from right
- Used `build-print-sizes.py` with `CROP_OFFSETS` to shift the default centered crop entirely onto the right or bottom edge
- `saltwash/listings/saltwash-beach-read-listing-draft.md` written
- README at `saltwash/beach-read/README.md` documents the crop math

### Pushed

Branch `beach-read-print` on `catherinebb272/blair-documents` at commit `9c0e28e`. Catherine can pull and start the Etsy upload.

## Operational note — .git/objects root-owned dirs

Hit a permission error mid-push: `.git/objects/{1c,13,63,fc}` are owned by root (probably from an earlier operation at 15:50 UTC today), and we can't chown them. Workaround: used `GIT_OBJECT_DIRECTORY=/tmp/git-alt-objs` + `GIT_ALTERNATE_OBJECT_DIRECTORIES` to write the new blobs to a writable alternate object store, then committed and pushed normally. Took ~3 minutes to figure out.

**Action for next session:** if the .git/objects root-ownership recurs, the same `GIT_OBJECT_DIRECTORY` workaround works. Cleaner long-term fix: have root chown the four dirs back to openclaw, but I don't have root here.

## Files in this print set

- 18×24 (master) — Etsy upload + source for the 4 derived sizes
- 16×24, 11×14, 8×10, 5×7 — derived via Lanczos
- Source JPG kept at `saltwash/beach-read/beach-read-18x24-source.jpg` (Catherine's Photoshop output)
