# 2026-04-25

## Etsy Check-In Cron Investigation

### Problem
Test cron `etsy-checkin-test-v2` was running but emails weren't being received automatically on schedule.

### Root Cause
The isolated cron session couldn't access the AgentMail API key. The `.env` file in the workspace wasn't being read by the cron job's isolated session.

### Fix Applied
Added API key directly to workspace `.env`:
```
AGENTMAIL_API_KEY=am_us_51e73bb1b1d943c447c17a3f409bb63878fef1145b82a1a27b57d4a98c607e9a
```

### Current Cron Config
- **ID:** c5b95218-0a87-4546-8edf-1bb1bfe1dbf9
- **Name:** etsy-checkin-test-v2
- **Schedule:** Every 1 hour
- **Session:** isolated
- **Message:** "Send a test email to apartxalone@gmail.com with subject 'TEST: Etsy Check-In (V2)' and body: 'This is V2 hourly test. Reply to confirm you got it.'"

### Test Results
| Time (UTC) | Status |
|------------|--------|
| ~22:58 (manual run) | ✓ Delivered |
| ~23:51 (auto) | ✗ Failed - API key missing |
| ~00:51 (auto) | ✗ Failed - API key missing |
| ~02:12 (manual run after .env fix) | ✓ Delivered |

### Testing
Next auto-run: ~02:51 UTC (9:51pm CT / 10:51pm ET)
Catherine will check inbox after this run.

### Key Learning
Use `--message` (not `--system-event`) when creating crons that need to take action. The isolated session needs credentials in .env.