# 2026-06-23

## Backup infrastructure — restored

The nightly workspace backup cron was lost during a cron cleanup 4 days ago. No backups had run from 2026-06-19 04:00 UTC through 2026-06-23 20:14 UTC.

### What I caught while investigating

Manual test backup push started committing a 1.7 GB Python venv (`tools/upscale/.venv/`) that had been created locally with no .gitignore rule to exclude it. Killed the push before it landed on the remote. Verified remote was still clean (last commit `aa78121` from 2026-06-19).

### Fixes applied

1. **`.gitignore` rewritten** with proper rules. Previous file had a stray `-e` typo at the top and was missing venv/backup exclusions. New rules:
   - Local-only state: `agent-library/`, `.last-backup`, `.trash/`, `.env`
   - Local mirrors / sub-repos: `blair-memory/`, `documents/heyron-tutorials/`, `heyron-tutorial-mirror/`, `heyron-tutorials/`
   - Logs: `logs/`
   - Python venvs: `**/.venv/`, `**/venv/`, `**/__pycache__/`, `**/*.pyc`
   - Root temp backups: `AGENTS.md.bak.*`, `SOUL.md.bak.*`, `TOOLS.md.bak.*`, `USER.md.bak.*`, `MEMORY.md.bak.*`, `IDENTITY.md.bak.*`
2. **`tools/upscale/.venv/` cleaned up** — git gc shrunk `.git/` from 1.2 GB to 407 MB after killing the aborted push. The venv directory itself is essentially empty now (just an `include/python3.11/` stub; gc reclaimed the orphaned objects).
3. **Nightly backup cron re-registered** as `workspace-nightly-backup` (`openclaw cron`, isolated session, `0 2 * * *` UTC). Next run: 2026-06-24 02:00 UTC. Failure DM goes to `discord:user:881612027863375872`.
4. **TOOLS.md updated** with current status, the re-register command for future use, and an explicit note that this host has no system cron — only `openclaw cron`.

### Manual pushes done today (to recover the gap)

- `49e4f0a` — Workspace backup 2026-06-23 20:21:30 UTC (.gitignore rewrite)
- `eb5cadd` — Workspace backup 2026-06-23 20:25:23 UTC (delete `Item Buy Detail Aug24-July25.csv`, 12 MB out-of-date Plano Closet LP data)
- `4f9584e` — Workspace backup 2026-06-23 20:28:23 UTC (delete `ItemBuyDetail_tanks.xlsx`, 630 KB, same category)

### Catch-up context (per Catherine, 20:55 UTC)

Yesterday (2026-06-22) Catherine uploaded **22 new draft listings** to Etsy as a catch-up push:
- 12 sober birthday (CSF)
- 5 comfy feelings (CFF)
- 2 sober birthday (duplicate mention — possibly typo)
- handful of other random stuff

**As of 20:55 UTC today, Drew has published 3 of the 22.** The other 19 remain in draft. Catherine says "now we are caught up" — the catch-up cycle is complete, Drew's publishing queue is the remaining work.

### Daily memory backfill (this file is part of it)

Backfilled 5 missing daily files:
- `memory/2026-06-19.md` — placeholder (no workspace evidence of work)
- `memory/2026-06-20.md` — **substantive**: documented 3 commits on `blair-documents/main` from that date (`e11da8c`, `4c39a06`, `b5370cf`) creating `documents/ana/greeting-cards/mockup-best-practices.md` — the 4-pose framework, master AI prompt template, Etsy slot allocation, and nano banana quirks & fixes from CTV work
- `memory/2026-06-21.md` — placeholder (no workspace evidence of work)
- `memory/2026-06-22.md` — placeholder (no workspace evidence of work)
- `memory/2026-06-23.md` — this file

### Notes for future-me

- The "Saltwash upscale tool" directory `tools/upscale/` exists locally but is empty (just the venv stub). Per Catherine's framing: the upscale work itself happens in Adobe Firefly / Upscayl / Topaz Gigapixel on Catherine's machine, not via a script here. The empty local directory is leftover; can be deleted or left.
- `Item Buy Detail Aug24-July25.csv` and `ItemBuyDetail_tanks.xlsx` — old Plano Closet LP data from April 2026, not related to ANA. Deleted from disk and from git tracking. Historical blobs still exist in git history (commit `770c174` and earlier) but won't appear in any new clone of HEAD.

### Side-issues flagged for later

- Daily memory files were missing 2026-06-19 → 2026-06-22 entirely (now backfilled as placeholders + 2026-06-20 with real content from `blair-documents` commits)
- `.last-backup` is from 2026-05-12 — stale. The file is gitignored, but worth updating after tonight's 02:00 UTC cron runs successfully
- The `.bak.*` backup files at root (`AGENTS.md.bak.portal-coder-connect-scrub-*`) are committed but obsolete — could be cleaned up if a real history scrub is wanted