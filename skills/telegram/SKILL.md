# Telegram Skill

## Purpose
Send messages and manage Telegram bot.

## Environment Variables
- `TELEGRAM_BOT_TOKEN` - Bot token

## Usage
Use the `message` tool with `channel: telegram` to interact with Telegram.