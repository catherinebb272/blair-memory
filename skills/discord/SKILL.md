# Discord Skill

## Purpose
Send messages and manage Discord channels via bot.

## Environment Variables
- `DISCORD_BOT_TOKEN` - Bot token

## Usage
Use the `message` tool with `channel: discord` to interact with Discord.